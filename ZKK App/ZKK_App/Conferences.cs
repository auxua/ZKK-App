using System;
using System.Collections.Generic;
using System.Text;

namespace ZKK_App
{
    /// <summary>
    /// A simple Enum representing the Conferences of the selector for all conferences.
    /// Could be extended for subsets, e.g. by ordinals or other stuff...
    /// </summary>
    public enum Conferences
    {
        ZAPF=1,
        KIF,
        KOMA,
        ALL
    }
}
